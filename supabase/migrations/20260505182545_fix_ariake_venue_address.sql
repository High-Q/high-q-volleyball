-- =============================================================================
-- 有明会場の住所表記を整える (Issue #90 動作確認時に発覚)
-- =============================================================================
-- 背景:
--   会員サイトのイベント詳細画面で会場住所を表示するようになった (#90) 際、
--   "〒135-0063 東京都江東区有明 1-8-14 先" の末尾「先」が会員視点で
--   違和感を与える表示になっていた。
--
--   「先」は登記地番表記で「当該番地の先 (隣接区画)」を意味するが、
--   一般ユーザー向けの住所案内としては不要。
--
-- 修正:
--   末尾「 先」を除去する。住所自体の正確性 (郵便番号 + 江東区有明 1-8-14)
--   は維持。
--
-- 関連:
--   supabase/migrations/20260428143738_db_schema_foundation.sql L110 (元 seed)
--   PR #202 (Issue #90 reservation イベント一覧・詳細画面) 動作確認フィードバック
-- =============================================================================

update public.venues
set address = '〒135-0063 東京都江東区有明 1-8-14'
where name = '有明会場';
