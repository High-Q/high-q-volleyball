{
  "name": "@high-q/edge-functions",
  "version": "0.0.0",
  "private": true,
  "description": "Supabase Edge Functions の共有ユーティリティ Vitest ランナー（Edge Function 本体は Deno で実行され、本 package は _shared/ のロジック層テスト目的でのみ存在する）",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.2.7"
  }
}
