## 1. 実 HTML の診断（原因の一次データ採取）

- [x] 1.1 `packages/court-crawler` に副作用のない診断モードを一時追加する（環境変数フラグで起動。LINE 送信・ストア更新をせず、対象日のグリッド構造だけを出力する）。出力は sanitize し、会場名・時刻・各セルのタグ名/class/空きシグナル（予約ボタン画像 `timetable-o.gif` / `onclick` の有無）だけを構造として出す。HTML 全文・認証情報・`g_sessionid`・PII は出力しない
- [ ] 1.2 診断モードを fix ブランチに push し、既存 workflow `court-crawler-koto.yml` を `workflow_dispatch`（feature branch から実行）で起動。空き枠が実在すると分かる直近の土日祝（例: 山の日・東砂スポーツセンター 大体育室 半面）を含む複数日をダンプする
- [ ] 1.3 ダンプ結果から、パースが依拠する 4 前提（時間帯の見出しセル / 会場名 / 空きセルの class / 日送り後のグリッド再取得）のどれが実サイトと食い違うかを特定し、原因が「セレクタ不一致」か「描画完了前の読み取り（タイミング）」かを結論づけて change に記録する

## 2. 根本修正（パース / driver）

- [ ] 2.1 【原因がセレクタ不一致の場合】空き判定を単一 class 依存から、意味的シグナル（予約ボタン画像 / `onclick` の存在）を主軸にした判定へ寄せ、class 名変更で再び静かに 0 件化しないようにする（`parse.ts`）。判定基準は 1.3 の実 DOM に合わせて確定
- [ ] 2.2 【原因が読み取りタイミングの場合】日送り後の各日について、表示日テキストだけでなく**空き状況セルの描画確定**まで待ってから読み取るよう driver を修正する（`driver.ts` の `collectAvailability` / `waitForDateChange` 相当。初回グリッド表示の確定待ちと同等の待機を各日に適用）
- [ ] 2.3 修正は同一 crawl コアを共有する別施設アダプタ（#287 Bumb）へ波及する形で行い、江東区固有値のハードコードに退行させない（純粋関数の責務分離を維持）

## 3. 回帰テスト（再発防止）

- [ ] 3.1 1.2 で採取した実 HTML を PII・セッション情報除去済みの新 fixture に落とす（`__fixtures__/` に「空きが実在する実構造」の fixture を追加）
- [ ] 3.2 新 fixture で「空きセルを正しく抽出する」ことを固定する回帰テストを `parse.spec.ts` に追加。空き 0 件しか拾えなくなったら赤くなることを確認する（TDD: 修正前に赤 → 修正後に緑）

## 4. 観測性（空き検知 funnel と静かな 0 の異常化）

- [ ] 4.1 crawl 結果サマリ（`CrawlSummary` / `collect` 戻り値）に段階ごとの件数を追加する: 読めたグリッド日数 / 絞り込み前の生の空き枠数 / 監視室場フィルタ後 / 土日祝・リードタイム絞り込み後（通知候補）/ 新規通知数 / 記録解除数（`crawl.ts` / `driver.ts` の `CollectResult`）
- [ ] 4.2 funnel を GitHub Actions のログと job summary（`court-crawler-koto.yml`）に出力し、DSN 未設定でも run 一覧でどの段で 0 に落ちたか追えるようにする
- [ ] 4.3 「読めたグリッド日数 > 0 かつ 生の空き枠数 = 0」を単一 run の異常として reporter に送る（グリッド 0 件の既存失敗とは文言・context で区別。`crawl.ts` + `failure.ts` / `sentry-reporter.ts`）。単一 run raw-0 を異常化する回帰テストを追加
- [ ] 4.4 Sentry 送出先（`KOTO_SENTRY_DSN`）設定の要否を運用手順として明記する（設定するかは翔太郎くんの判断を仰ぎ、決定に沿って docs / workflow コメントを更新）

## 5. 診断足場の撤去と最終確認

- [ ] 5.1 1.1 の診断モード（一時足場）を撤去し、恒久観測（funnel + 静かな 0 異常化）だけを残す。生 HTML をログに残す経路が本番に残っていないことを確認
- [ ] 5.2 `pnpm --filter @high-q/court-crawler exec vitest run` を実行し全テスト緑を確認。lint / typecheck も通す
- [ ] 5.3 修正ブランチを dispatch で実 run し、空き枠が実在する状態でオーナーに LINE 通知が届くことを確認する（`monitor_all` で生の funnel が非 0 になること、通常運用フィルタで監視室場の空きが通知されること）
